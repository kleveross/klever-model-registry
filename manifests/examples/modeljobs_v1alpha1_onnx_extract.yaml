apiVersion: kleveross.io/v1alpha1
kind: ModelJob
metadata:
  name: modeljob-onnx-extract
  namespace: default
spec:
  # Add fields here
  model: "harbor-harbor-core.kleveross-system/release/onnx:v1"
  extraction:
    format: "ONNX"